# Print all commands to log before executing them.
echo on
# Initialize RS plugin
plugin -i synth-rs
# Read HDL files
${READ_HDL}

# perform the synthesis
synth_rs -top ${TOP_MODULE} -tech genesis -goal delay -de -no_dsp -no_bram -verilog ${TOP_MODULE}.verilog
