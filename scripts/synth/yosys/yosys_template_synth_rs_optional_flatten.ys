# Print all commands to log before executing them.
echo on
# Initialize RS plugin
plugin -i synth-rs
# Read HDL files
${READ_HDL}

verific -import -flatten ${TOP_MODULE}

# perform the synthesis
synth_rs ${OPTIONS}
